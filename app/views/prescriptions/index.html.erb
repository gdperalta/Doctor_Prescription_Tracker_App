<p id="notice"><%= notice %></p>
<h1>Prescriptions</h1>
<table>
	<thead>
		<tr>
			<th>Total cost</th>
			<th>Doctor</th>
			<th colspan="3"></th>
		</tr>
	</thead>
	<tbody>
		<% @prescriptions.each do |prescription| %>
			<tr>
				<td><%= prescription.total_cost %></td>
				<td><%= prescription.doctor_id %></td>
				<td><%= link_to 'Show', doctor_prescription_path(prescription) %></td>
				<td><%= link_to 'Edit', edit_doctor_prescription_path(prescription) %></td>
				<td><%= link_to 'Destroy', prescription, method: :delete, data: { confirm: 'Are you sure?' } %></td>
			</tr>
		<% end %>
	</tbody>
</table>
<br>
<%= link_to 'New Prescription', new_doctor_prescription_path %>
