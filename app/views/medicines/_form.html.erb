<%= form_with(model: medicine) do |form| %>
	<% if medicine.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(medicine.errors.count, "error") %> prohibited this medicine from being saved:</h2>
			<ul>
				<% medicine.errors.each do |error| %>
					<li><%= error.full_message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>
	<div class="field form-floating mb-3">
		<%= form.text_field :meds_name, class: 'form-control', placeholder: 'Medicine name' %>
		<%= form.label :meds_name, 'Medicine Name', class: 'form-label' %>
	</div>
	<div class="field form-floating mb-3">
		<%= form.number_field :meds_cost, class: 'form-control', placeholder: 'Cost in PHP' %>
		<%= form.label :meds_cost, 'Cost', class: 'form-label'  %>
	</div>
	<div class="field form-floating mb-3">
		<%= form.text_field :meds_category, class: 'form-control', placeholder: 'Medicine Category' %>
		<%= form.label :meds_category, 'Category', class: 'form-label'  %>
	</div>
	<div class="actions">
		<%= form.submit class: 'btn btn-outline-success' %>
	</div>
<% end %>
